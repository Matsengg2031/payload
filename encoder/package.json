{
  "name": "payload-encoder",
  "version": "1.0.0",
  "description": "Encoder tools for payload delivery",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "pngjs": "^6.0.0"
  }
}
